dependencies:
  - base
  - patches-vector
  - semigroups
  - containers
  - vector

ghc-options:
  -Wall -fno-warn-name-shadowing

library:
  source-dirs:
    - src

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - base-compat
      - hspec
      - QuickCheck
      - checkers
      - hspec-checkers
      - quickcheck-instances
